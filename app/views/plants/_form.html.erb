<!-- COMMON NAME -->
<%= form_for plant do |f| %>
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Common name:*<br>
      <%= f.text_field :common_name, class: "form-control" %><br>
    </div>
  </div>

<!-- LATIN NAME -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Latin name:<br>
      <%= f.text_field :latin_name, class: "form-control" %><br>
    </div>
  </div>

<!-- VARIETY -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Variety:*<br>
      <%= f.text_field :variety, class: "form-control" %><br>
    </div>
  </div>

<!-- HEIGHT -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Height:<br>
      <%= f.text_field :height, class: "form-control" %><br>
    </div>
  </div>

<!-- LIGHT -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Light requirements:<br>
        <%= f.select :light, options_for_select([['Select one', ''], 'Full sun', 'Part sun', 'Shade']), {}, class: "form-control" %><br>
    </div>
  </div>

<!-- LIFE CYCLE -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Lifecycle:<br>
        <%= f.select :lifecycle, options_for_select([['Select one', ''], 'Annual', 'Perennial']), {}, class: "form-control" %><br>
    </div>
  </div>

<!-- SPACING -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Spacing (inches):<br>
        <%= f.text_field :spacing, class: "form-control" %><br>
    </div>
  </div>

<!-- SEASONS -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Sowing season:*<br>
        <%= f.collection_check_boxes(:season_ids, Season.all, :id, :season) do |b| %>
        <div class="row">
          <%= b.label(class: "check_box") do %>
            <div class="col-xs-4">
              <%= b.check_box(class: "check_box") %>
            </div>
            <div class="col-xs-6">
              <%= b.object.season %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>

<p>
<!-- DAYS TO MATURITY -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Days to maturity:<br>
        <%= f.text_field :days_to_maturity, class: "form-control" %><br>
    </div>
  </div>

<!-- TAGS -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">

      <p class="bold">Tags:<br>
      <%= f.collection_check_boxes(:tag_ids, Tag.all, :id, :name) do |b| %>
        <div class="row">
          <%= b.label(class: "check_box") do %>
            <div class="col-xs-4">
              <%= b.check_box(class: "check_box") %>
            </div>
            <div class="col-xs-6">
              <%= b.object.name %>
            </div>
          <% end %>
        </div>
      <% end %>

      <p>New tag:
        <!-- add feature so tag cannot be blank -->
      <%= f.fields_for :tags, plant.tags.build do |tags_fields| %>
        <%= tags_fields.text_field :name %><br>
      <% end %>

     </div>
   </div>

<p>
<!-- IMAGE-->
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p class="bold">Image URL:<br>
        <%= f.text_field :image, class: "form-control" %><br>
    </div>
  </div>

 <div class="row">
   <%= f.submit class: 'btn' %>
 </div>
<% end %>
